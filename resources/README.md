# App assets for Capacitor

Place these files here, then run from the project root:

```bash
npm run assets
```

Required files:

- `icon.png` — 1024×1024 (app launcher icon)
- `splash.png` — 2732×2732 (splash image; centered on brand purple `#714b67`)
