<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <include
        android:id="@+id/onboarding_screen"
        layout="@layout/layout_onboarding"
        android:visibility="gone" />

    <include
        android:id="@+id/login_screen"
        layout="@layout/layout_login"
        android:visibility="gone" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/header_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@color/surface"
            android:clipToPadding="false"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <LinearLayout
                android:id="@+id/header_bar"
                android:layout_width="0dp"
                android:layout_height="@dimen/header_content_height"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="@dimen/header_horizontal_padding"
                android:paddingEnd="@dimen/header_horizontal_padding"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <ImageView
                    android:id="@+id/header_home_icon"
                    android:layout_width="@dimen/header_home_icon_size"
                    android:layout_height="@dimen/header_home_icon_size"
                    android:background="?android:attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/header_logo_home"
                    android:importantForAccessibility="yes"
                    android:scaleType="fitCenter"
                    android:src="@drawable/ic_header_home" />

                <TextView
                    android:id="@+id/header_home_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/header_home_gap"
                    android:background="?android:attr/selectableItemBackgroundBorderless"
                    android:fontFamily="sans-serif-medium"
                    android:text="@string/header_home_title"
                    android:textColor="@color/brand_black"
                    android:textSize="16sp" />
            </LinearLayout>

            <View
                android:id="@+id/header_divider"
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:background="@color/header_border"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/header_bar" />
        </androidx.constraintlayout.widget.ConstraintLayout>

        <include
            android:id="@+id/bottom_nav"
            layout="@layout/layout_bottom_nav"
            android:layout_width="0dp"
            android:layout_height="@dimen/bottom_nav_total_height"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <FrameLayout
            android:id="@+id/webview_container"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:clipChildren="true"
            android:clipToPadding="true"
            app:layout_constraintBottom_toTopOf="@id/bottom_nav"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/header_container">

            <WebView
                android:id="@+id/odoo_webview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:overScrollMode="ifContentScrolls"
                android:scrollbars="vertical" />
        </FrameLayout>

        <include
            android:id="@+id/menu_overlay"
            layout="@layout/layout_menu_overlay"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            app:layout_constraintBottom_toTopOf="@id/bottom_nav"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/header_container" />

        <include
            android:id="@+id/legal_overlay"
            layout="@layout/layout_legal_overlay"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:elevation="12dp"
            android:visibility="gone"
            app:layout_constraintBottom_toTopOf="@id/bottom_nav"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/header_container" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>
