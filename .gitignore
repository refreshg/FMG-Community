# Dependencies
node_modules/

# Capacitor local overrides (keep capacitor.config.json)
capacitor.config.local.*

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Android build outputs (keep android/ source under app/src, Gradle files, etc.)
android/build/
android/app/build/
android/.gradle/

# Android local / signing (never commit secrets)
local.properties
keystore.properties
*.keystore
*.jks

# Firebase (project-specific; add locally)
android/app/google-services.json
